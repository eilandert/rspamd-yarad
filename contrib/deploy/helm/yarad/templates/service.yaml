apiVersion: v1
kind: Service
metadata:
  name: {{ include "yarad.fullname" . }}
  labels:
    {{- include "yarad.labels" . | nindent 4 }}
spec:
  type: {{ .Values.service.type }}
  ports:
    - name: http
      port: {{ .Values.service.port }}
      targetPort: http
      protocol: TCP
  selector:
    {{- include "yarad.selectorLabels" . | nindent 4 }}
