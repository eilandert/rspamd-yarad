apiVersion: v1
kind: Service
metadata:
  name: {{ include "mailstrix.fullname" . }}
  labels:
    {{- include "mailstrix.labels" . | nindent 4 }}
spec:
  type: {{ .Values.service.type }}
  ports:
    - name: http
      port: {{ .Values.service.port }}
      targetPort: http
      protocol: TCP
  selector:
    {{- include "mailstrix.selectorLabels" . | nindent 4 }}
